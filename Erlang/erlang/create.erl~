%creating lists assignemnt.

-module(create).

-export([create/1, reversec/1]).

%Create a list given N, i.e. create(3), List = 1,2,3 etc..


create(X) when X > 0 ->
    [create(X) | X].

reversec(X) ->
    [X | create(X -1)].
